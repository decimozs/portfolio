pre-commit:
  parallel: true
  commands:
    biome-check:
      glob: "**/*.{js,ts,cjs,mjs,d.cts,d.mts,jsx,tsx,json,jsonc}"
      run: bunx biome check --write --no-errors-on-unmatched --files-ignore-unknown=true --colors=off {staged_files} && bun run prepush
      stage_fixed: true
